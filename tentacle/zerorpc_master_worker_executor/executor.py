class DistributedWorkerTeamProvider(object):
    def create_team_for_processing(self, communication_dir, logger_provider):
        #result should support process(tasks)]
        pass
    def find_team_for_joining(self, communication_dir):
        #result should support join_and_work(worker) 
        pass
